export type Pokemon = {
	name: string;
	url: string;
	image: string;
};
export type PokemonUrl = {
	name: string;
	url: string;
};
export type LayoutType = {
	title: string;
	keywords?: string;
	description?: string;
	children: any;
};
